/**
 * Service orchestration (start/stop/restart) will call PowerShell helpers in a later iteration.
 * v0.1 uses `health-check.ps1` + per-service `check-*.ps1` scripts for state.
 */
export const servicesPlaceholder = true
